- name: Install git
  ansible.builtin.apt:
    name: git
    state: present

- name: Copy gitconfig
  ansible.builtin.copy:
    src: .gitconfig
    dest: "{{ ansible_env.HOME }}/.gitconfig"
    owner: "{{ ansible_env.USER }}"
    group: "{{ ansible_env.USER }}"
    mode: "0644"
